# Task: Enable Media Serving in Production

- [x] Identify issue: `urls.py` only serves media when `DEBUG=True`.
- [x] Modify `urls.py` to use `django.views.static.serve` for `MEDIA_URL` when `DEBUG=False`.
- [x] Commit and push changes.
